-- SELECT 
--         count(*),
--         count(1),
--         count(idCliente),
--         count(DtCriacao),
--         count(DtAtualizacao)

-- FROM clientes

SELECT DISTINCT flTwitch, flEmail -- O distinct trás as LINHAS distintas da COMBINAÇÃO DE COLUNAS colocadas no SELECT

FROM clientes